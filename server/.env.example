# Environment Configuration
NODE_ENV="development" # Options: 'development', 'production'
PORT="8083"            # The port your server will listen on
HOST="localhost"       # Hostname for the server

# CORS Settings
# CORS_ORIGIN="http://localhost:8083" # Allowed CORS origin, adjust as necessary
CORS_ORIGIN="*" # Allowed CORS origin, adjust as necessary

# Rate Limiting
COMMON_RATE_LIMIT_WINDOW_MS="1000"  # Window size for rate limiting (ms)
COMMON_RATE_LIMIT_MAX_REQUESTS="200" # Max number of requests per window per IP

SERMAS_URL=http://localhost:8080
SERMAS_CLIENT_ID=foo
SERMAS_CLIENT_SECRET=bar
SERMAS_USER=admin
SERMAS_PASSWORD=admin